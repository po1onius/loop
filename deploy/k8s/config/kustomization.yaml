apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: loop
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: loop-env
    envs:
      - runtime.env
  - name: loop-api-config
    files:
      - loop-api-svc.toml
